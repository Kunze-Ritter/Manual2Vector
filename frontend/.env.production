# ============================================================================
# KRAI Frontend - Production Environment Configuration
# ============================================================================
# 
# This file contains production-specific environment variables for the KRAI
# frontend application. These values are BAKED INTO THE BUILD at compile time
# by Vite, not read at runtime.
#
# IMPORTANT: This file SHOULD be committed to git (unlike .env.local)
#
# For detailed deployment information, see:
# docs/setup/FRONTEND_PRODUCTION_DEPLOYMENT.md
# ============================================================================

# ----------------------------------------------------------------------------
# API Configuration
# ----------------------------------------------------------------------------
# Base URL for API requests. Uses relative path to leverage nginx proxy.
# The nginx configuration (nginx/nginx-simple.conf) forwards /api/* requests
# to the backend at http://krai-engine:8000/
#
# Production: /api (relative path through nginx proxy)
# Development: /api (through Vite dev proxy) or http://localhost:8000 (direct)
VITE_API_BASE_URL=/api

# API request timeout in milliseconds (30 seconds)
# Matches development timeout for consistency
VITE_API_TIMEOUT=30000

# ----------------------------------------------------------------------------
# Authentication Storage Keys
# ----------------------------------------------------------------------------
# LocalStorage keys for storing authentication tokens
# These must match the keys expected by the backend authentication system
VITE_TOKEN_STORAGE_KEY=krai_auth_token
VITE_REFRESH_TOKEN_STORAGE_KEY=krai_refresh_token

# ----------------------------------------------------------------------------
# Feature Flags
# ----------------------------------------------------------------------------
# Enable React Query DevTools
# MUST be false in production for performance and security
# DevTools add ~50KB to bundle and expose internal query state
VITE_ENABLE_DEVTOOLS=false

# Use mock authentication API (for testing/development only)
# MUST be false in production to use real authentication
VITE_USE_MOCK_AUTH=false

# ============================================================================
# Build-Time vs Runtime Variables
# ============================================================================
# 
# Vite Environment Variables (prefixed with VITE_):
# - Read at BUILD TIME and baked into the JavaScript bundle
# - NOT available at runtime - values are compiled into the code
# - Changing these requires rebuilding the Docker image
#
# To rebuild after changing these values:
#   docker-compose -f docker-compose.production-final.yml build krai-frontend
#
# ============================================================================
