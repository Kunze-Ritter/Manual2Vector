# ==========================================
# HOST-MACHINE ENVIRONMENT OVERRIDES
# ==========================================
# Copy this file to `.env.local` when running backend services directly on the
# host (outside of Docker). The `.env` file continues to target Docker service
# hostnames such as `krai-postgres`, while `.env.local` remaps them to localhost
# so binaries running on the host can communicate with the containers via
# published ports.
#
# IMPORTANT:
# - `.env.local` is ignored by git. Do NOT commit populated secrets.
# - This file only needs the variables you want to override. Everything else is
#   inherited from `.env`.

# ------------------------------------------
# DATABASE (Host ↔ Docker bridge)
# ------------------------------------------
DATABASE_HOST=localhost
POSTGRES_HOST=localhost
DATABASE_CONNECTION_URL=postgresql://krai_user:krai_secure_password@localhost:5432/krai

# ------------------------------------------
# OBJECT STORAGE (optional overrides)
# Uncomment if you access MinIO via published localhost ports instead of
# docker-internal DNS names.
# ------------------------------------------
#OBJECT_STORAGE_ENDPOINT=http://localhost:9000
#OBJECT_STORAGE_PUBLIC_URL=http://localhost:9000

# ------------------------------------------
# AI SERVICE (optional overrides)
# ------------------------------------------
#AI_SERVICE_URL=http://localhost:11434
#OLLAMA_URL=http://localhost:11434
